{
  "name": "personal-website",
  "version": "0.0.1",
  "description": "Experimentation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-css": "node_modules\\.bin\\node-sass --watch --recursive --output public/css --source-map true --source-map-contents scss"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.5.0"
  }
}
